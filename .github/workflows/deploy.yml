name: deploy

on:
  push:
    tags:
      - "v*"

jobs:
  deploy:
    runs-on:
      - self-hosted
      - linux

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set deploy variables
        run: |
          echo "IMAGE_NAME=telegram-sender-api" >> "$GITHUB_ENV"
          echo "IMAGE_TAG=${GITHUB_REF_NAME}" >> "$GITHUB_ENV"
          echo "DEPLOY_ENV_FILE=/opt/telegram-sender-api/.env" >> "$GITHUB_ENV"
          echo "APP_BIND_IP=${{ vars.APP_BIND_IP }}" >> "$GITHUB_ENV"

      - name: Verify deploy variables
        run: bash scripts/run_make.sh verify-deploy-env

      - name: Verify Docker
        run: bash scripts/run_make.sh verify-docker

      - name: Quality gate
        run: bash scripts/run_make.sh docker-check

      - name: Deploy
        run: bash scripts/run_make.sh deploy-release
